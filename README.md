# 文明 6 区域规划

> 一个**高保真还原《文明 VI》产出规则**的区域规划工具与残局关卡集。
> 面向 **系统策划 + 数值策划** 岗位的作品集项目。
> 数据版本：文明 VI 1.4.6（buildid 15296837）完整版，提取日 2026-09-24。

---

## 这个项目想证明什么

不是"我做了一个像文明 6 的游戏"，而是：

> **我把文明 6 的产出规则精确还原到可对账的程度，并在其上做了关卡设计与数值标定。**

所以这个仓库里最值得读的不是玩法，是**规则是怎么被查实的**，以及**查错之后文档是怎么被改的**。

---

## 五分钟路线（推荐按这个顺序看）

| # | 读什么 | 为什么这份最能说明问题 |
|---|---|---|
| 1 | [拆解/文明6-产出与相邻加成体系.md](拆解/文明6-产出与相邻加成体系.md) §2.2 | **文明 6 的区域自身零产出**，全部 36 个区域的基础产出都是「无」——所以相邻加成**就是**产出。这个结论推翻了我原本的建模 |
| 2 | [设计/SDD-局面求值器.md](设计/SDD-局面求值器.md) §3.3 | `增量 = 相邻符合数 × 加成值 ÷ 所需数量`，**全程不取整**。这个公式是**在游戏里实测出来的**，不是从数据里读出来的 —— 原因见下 |
| 3 | [拆解/文明6-产出与相邻加成体系.md](拆解/文明6-产出与相邻加成体系.md) §4.4 | **为什么算法只能靠实测**：规则与数值全在 XML 里（可读），但计算方式在编译后的 C++ gameplay core 里，而文明 6 与文明 5 不同，**没有开放 gameplay DLL 源码** |
| 4 | [设计/关卡设计.md](设计/关卡设计.md) §2.0 + §2.0.1 | 关卡质量有一个**可计算的定义**（贪心必须失败），以及一条量化判据 `母题成立 ⟺ V > w × min(m,k)`。四个旋钮 `w/m/k/V` 可直接用 |
| 5 | [设计/数值设计.md](设计/数值设计.md) §1.1 | **同一个事实被两条独立路径验证**：关卡侧测出"韩国结构性地不可能让贪心失败"，数据侧算出"书院是全表唯一相邻上界与邻格无关的区域" |

想看**我改错的记录**（这个项目最想展示的部分）：[记录/进度记录.md](记录/进度记录.md) 是一份只追加的事实日志，每条都写了当时错在哪、怎么发现的、连带改了什么。

---

## 目录

```
区域规划demo/
├── 拆解/                     ← 文明 6 是怎样的（作品集 #2）
│   ├── 文明6-产出与相邻加成体系.md   ★ 最重要
│   ├── 文明6-区域与建筑.md
│   ├── 文明6-文明与领袖特色.md
│   └── 文明6-单位与军事叠力.md      ⚠️ 尚为二手，待游戏内实测
├── 设计/                     ← 我们的游戏怎么做（作品集 #4）
│   ├── 立项书.md
│   ├── GDD总纲.md                系统之间怎么咬合 + 验证指标
│   ├── SDD-局面求值器.md          ★ 核心系统，29 条边界情况
│   ├── SDD-自由模式.md
│   ├── SDD-挑战模式.md
│   ├── 关卡设计.md                母题、判据、贪心基线的精确定义
│   ├── 数值设计.md                产出量级、相邻 vs 建筑、难度曲线
│   └── 测试清单.md                数值对账、通关路径、回归影响矩阵
├── 配置表/                   ← 唯一事实来源，由解析脚本生成
│   ├── 字段说明.md                17 张表、21 条校验规则
│   └── *.csv
├── Tools/                    ← 全部只用 Python 标准库
└── 记录/进度记录.md           ← 只追加的事实日志
```

---

## 怎么跑

全部工具**只用 Python 标准库**（系统 `python3` 3.9 即可，不需要建 venv、不需要 pip install）。

```bash
python3 Tools/check_config.py              # 配置表一致性校验（21 条规则）
python3 Tools/test_check_config.py         # 校验器自己的回归测试（66 条断言）
python3 Tools/analyze_yields.py            # 产出量级分析（数值设计.md 的数字来源）
python3 Tools/design_levels.py             # 重算关卡目标值与星级阈值，重新生成关卡表
python3 Tools/motif_check.py               # 关卡母题验证（先校准，校准不过就停）
```

需要装了文明 6 才能跑的：

```bash
python3 Tools/parse_civ6_xml.py            # 游戏 XML → 配置表 CSV
```

**可复现性是这套工具的硬指标**：删掉 `配置表/*.csv` 再跑一次 `parse_civ6_xml.py`，13 张生成表逐字节一致。

---

## 当前状态

| 部分 | 状态 |
|---|---|
| 拆解案 | 3/4 篇由一手数据填实；军事篇仍为二手，标 `待核=是` |
| 配置表 | 13 张生成表 + 2 张关卡表；`units` / `combat_modifiers` 阻塞于军事实测 |
| 关卡 | **10 关全部落地**：3 教学 + 5 普通 + 1 对照（另 1 关作废但留作回归用例） |
| 求值器 | 规格完成（29 条边界情况）；设计期原型已写，**产品代码未开始** |
| 两模式 UI | 规格完成；**未实现** |
| 技术栈 | 🔴 **未定**（TS+Web 还是 Unity C#）。文档层全部 engine-neutral |

表行数：`adjacency_rules` 103、`buildings` 85、`districts` 36、`civs` 100、`leaders` 126、`techs` 77、`civics` 61、`improvements` 59、`resources` 54、`features` 50、`wonders` 34、`terrains` 17、`excluded_adjacencies` 12。

---

## 这个项目的工作方式（三条自己给自己立的规矩）

### 1. 数值只能来自一手数据

所有数字来自游戏本体的 XML，或**游戏内实测**。二手资料（wiki、攻略）可以临时占位，但必须标 `待核=是`，而 `check_config.py --delivery` 会**拦住任何 `待核=是` 的行**进入对外产物。

理由不是洁癖：拆解过程中二手资料至少有两处与一手数据冲突（军事篇的受损惩罚曲线；"文明 6 没有负向相邻"这句话本身就是错的——韩国书院就是）。

### 2. 判据必须可计算，而且必须真的拿去跑

文档里写"最优布局结构性改变"是一句愿望，写"交叉代入双向劣化 ≥ 0"才是判据。这个项目里有两条验证指标**被实测逼着从愿望改成了判据**，记录在 [设计/GDD总纲.md](设计/GDD总纲.md) §6.1。

> **指标一旦真的拿去跑，就会暴露它到底可不可计算。**

### 3. 错了就改文档，并把"为什么会错"留在原处

这个项目删掉了两个原本写好的关卡母题（A「争格」、D「取整浪费」），都保留了原节作为记录——因为它们各自记下了一次真实的方法论失误：一次是判据不够严导致的假阳性，一次是建立在未验证机制上的设计整块作废。

同类记录还有：把 L-06 做不出来归因为"地图太小"（错的，见 [设计/关卡设计.md](设计/关卡设计.md) §4.1；真正原因是我的协同块形状不对，几何搜索证明这张图有 14 万种可行摆法）；把圣地的建筑高算倍数手算成 3.7 倍（实际 4.1~5.0 倍）。

---

## 已知的空缺（写在明面上）

- **军事整块阻塞**：`units.csv` / `combat_modifiers.csv` 未生成，母题 F（地形与支援的取舍）无法设计。阻塞项是两条需要游戏内实测的规则：多个大将军是否叠加、受损惩罚曲线
- **`districts.OnePerCity` / `MaxPerPlayer` 未补**：所以 [设计/数值设计.md](设计/数值设计.md) §1 的相邻上界是「忽略全局唯一性」的上界
- **预算 6+ 的关卡做不了**：穷举最优在预算 6 以上吃不住，需要束搜索求解器（在暂缓清单里）
- **多产出关卡的难度度量不够好**：`V−G`（合计差）在多维目标上失真，见 [设计/数值设计.md](设计/数值设计.md) §4.1
- **产品代码一行没写**：技术栈未定。这是有意的——规格先定，栈后定，文档层保持 engine-neutral

---

## 与本仓库其他 demo 的关系

`gamejam/` 下另有 `武侠RPGdemo`（回合制战斗垂直切片）。两个项目在**信息设计上给出了相反的答案**，而且各有理由：

| | 武侠 RPG | 区域规划 |
|---|---|---|
| 敌人弱点 / 规则 | **初始隐藏**，命中后永久显形 | **全部公开** |
| 理由 | 试错本身是核心玩法 | 算清楚才是核心玩法 |

同一个问题两个相反答案、且都能说出理由——这个对照本身是作品集素材，写在 [设计/GDD总纲.md](设计/GDD总纲.md) §4。
